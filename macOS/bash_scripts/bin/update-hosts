#!/usr/bin/env bash
set -euo pipefail

sudo vim -n -Es -u NONE -i NONE -c '
  silent! /^# Title: StevenBlack\/hosts/
  if v:errmsg != ""
    cquit
  endif
  set nomodified
  ., $d
  r /tmp/hosts.txt
  if &modified | wq | else | q! | endif
' /etc/hosts
