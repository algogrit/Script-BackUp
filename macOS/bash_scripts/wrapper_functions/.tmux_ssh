function tssh() {
    local session="Gaurav"

    if [[ "$1" == "-d" ]]; then
        session="Gaurav-$(basename $(pwd))"
        shift
    fi

    ssh "$@" -t "tmux a -t '$session' || tmux new -s '$session' || screen -r || screen || /bin/bash"
}
